# Pinned so local builds use the exact toolchain CI does. Bump this AND the
# `dtolnay/rust-toolchain@<ver>` refs in .github/workflows/*.yml together.
[toolchain]
channel = "1.95.0"
components = ["clippy", "rustfmt"]
